<% if object && object.errors.size > 0 %>
  <div id="error_explanation">
    <h2><%= pluralize(object.errors.size, "error") %> prohibited this object from being saved, man </h2>
    <p>Problems with these fields:</p>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>